import { generateComboTexts } from '../editor/Store/sampleTags'

const ilinkStrings: string[] = ['github', 'workduck', 'mex', 'dev', 'testing', 'deployment', 'javascript']

export default generateComboTexts(ilinkStrings)
