import React, { useState } from 'react'

interface AccordionProps {
  preOpen?: boolean
}

const Accordion = ({ preOpen }: AccordionProps) => {
  const [open, setOpen] = useState(false)

  return <div>

  </div>
}

export default Accordion
